# Verifier Packet

Use this packet to review one candidate experiment against a baseline experiment.

Reply constraints:
- Return YAML only.
- Base the verdict only on the provided experiment artifacts and metric deltas.
- Keep issues and next changes concrete and auditable.
